<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/code_icon"
        android:label="alpermelkeli"
        android:supportsRtl="true"
        android:theme="@style/Theme.PersonalBlog"
        tools:targetApi="31">
        <activity
            android:name=".uı.skills.SkillsActivity"
            android:exported="false" />
        <activity
            android:name=".uı.experience.ExperienceActivity"
            android:exported="false" />
        <activity
            android:name=".uı.education.EducationActivity"
            android:exported="false" />
        <activity
            android:name=".uı.projects.ProjectsActivity"
            android:exported="false" />
        <activity
            android:name=".uı.navigate.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>