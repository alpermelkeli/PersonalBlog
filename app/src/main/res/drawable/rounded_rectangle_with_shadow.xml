<!-- res/drawable/soft_shadow_background.xml -->

<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- Arka plan rengi -->
            <corners android:radius="20dp" /> <!-- Köşe yarıçapı -->
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" /> <!-- Gölge rengi -->
            <corners android:radius="20dp" /> <!-- Köşe yarıçapı (aynı olmalı) -->
        </shape>
    </item>
</layer-list>
